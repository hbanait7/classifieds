<h1>Products#index</h1>

<% @products.each do |product| %>

  <%= product.price %>
  <%= link_to product.name, product_path(product) %>
  <%= product.category %>
  <%= product.description %>

<% end %>
